# 2026-07-24

- Added `claude.md` with developer guidelines.
- Removed deprecated `CalendarEventsView` and `CalendarEventDetailView`.
- Scaffolding `common/` API infrastructure (ApiResponse, ApplicationError).
- Implemented `Dashboard` module (`DashboardAppointmentsView`, `DashboardStatsView`, `DashboardSelector`).
- Refactored `accounts`, `chatbot`, and `calendar_app` apps to strictly use `ApiResponse` envelope.
- Extracted business logic in `chatbot` to `ChatSessionService` and `AgenticService`.
- Added `PATCH /api/v1/accounts/me/` for provider profile updates.
