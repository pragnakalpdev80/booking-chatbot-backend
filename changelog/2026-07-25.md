# 2026-07-25

## Bug Fixes
- Fixed payment bypass: `book_appointment` is now strictly removed from LLM tool schema when `ProviderSettings.payment_required = True`. Added backend guardrail in `_book_appointment`.

## Features
- Added `GET /api/v1/accounts/providers/` public endpoint listing all staff users as providers.
- Added `ProviderSelector` component — always shown before chat loads; user must select a provider explicitly (even if only one exists). Replaces hardcoded `provider_id: 1`.
