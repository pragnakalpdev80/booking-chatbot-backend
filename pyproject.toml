[project]
name = "calendar-chatbot"
version = "1.0.0"
description = "Calendar Chatbot Backend"
requires-python = ">=3.12"

# --------------------------------------------------------------------------- #
# Ruff: linting + formatting (single tool, black-compatible)
# --------------------------------------------------------------------------- #
[tool.ruff]
line-length = 100
target-version = "py312"
extend-exclude = ["migrations", "venv", ".venv", "../venv"]

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "F",    # pyflakes
    "I",    # isort
    "UP",   # pyupgrade
    "B",    # flake8-bugbear
    "DJ",   # flake8-django
    "C4",   # flake8-comprehensions
    "SIM",  # flake8-simplify
]
ignore = []

[tool.ruff.lint.per-file-ignores]
# Pre-existing long lines and raise-without-from in these files
"apps/calendar_app/services/gcal_client.py" = ["E501"]
"apps/payments/services/webhook_service.py" = ["B904"]
"apps/payments/tasks.py"                    = ["E501", "B904"]
"apps/payments/tests/*.py"                  = ["E501"]
"apps/payments/urls.py"                     = ["E501"]
"apps/payments/views.py"                    = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["config", "apps"]

[tool.ruff.format]
quote-style = "double"

# --------------------------------------------------------------------------- #
# mypy: gradual typing
# --------------------------------------------------------------------------- #
[tool.mypy]
python_version = "3.12"
plugins = ["mypy_django_plugin.main"]
ignore_missing_imports = true
warn_unused_ignores = true
exclude = "(migrations|venv|\\.venv|../venv)"

[[tool.mypy.overrides]]
module = ["config.*"]
disallow_untyped_defs = true

[tool.django-stubs]
django_settings_module = "config.settings.local"

# --------------------------------------------------------------------------- #
# pytest + coverage
# --------------------------------------------------------------------------- #
[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "config.settings.local"
python_files = ["test_*.py", "tests.py", "*_tests.py"]
addopts = "--cov=apps --cov=config --cov-report=term-missing -v"
testpaths = ["apps", "tests"]

[tool.coverage.run]
relative_files = true
omit = [
    "*/migrations/*",
    "*/tests/*",
    "manage.py",
    "config/asgi.py",
    "config/wsgi.py",
    "config/schema.py",
]

# --------------------------------------------------------------------------- #
# bandit: static security analysis
# --------------------------------------------------------------------------- #
[tool.bandit]
exclude_dirs = ["venv", ".venv", "../venv", "migrations", "tests"]
skips = ["B101"]
